const lexer = ({ code, dictionary }: { code: string, dictionary: Object }) => {
	let tmp: string = "";
	let last: string = "";
	let buffer: string[] = code.split("");
	for(let i: number = 0; i < buffer.length; i++){
		while(){}
	}
};

export default lexer;
